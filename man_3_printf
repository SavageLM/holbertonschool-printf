.TH _printf 1 "7 July 23"
.SH NAME
_printf /- Function that will replicate the std printf function.
.SH SYNOPSIS
.nf
.B #include "main.h"
.PP
.BI "int _printf(const char *" format ", ...);"
.fi
.SH DESCRIPTION
   The function _printf() produces output according to
   a format as described below. The function _printf()
   writes output to stdout, the standard output stream.
   These functions write the output under the control of a 
   format string that specifies how subsequent arguments
   are converted for output. The format is a character	
   string and is composed of zero or more directives.
.SH RETURN VALUES
   This function returns the number of characters printed. 
   This function returns a negative value if an error 
   occurs.

.SH Options

.RS
.nf
Specifiers
- %c - Prints a single character.
- %s - Prints the argument as a string.
- %i - Prints signed decimal integer. 
- %d - Prints signed decimal integer.
.RE

.SH "SEE ALSO"
_printf.c
_putchar.c
helper_func.c
main.h
man_3_printf
print_char.c
print_num.c
print_string.c

.SH BUGS
Eww.

.SH AUTHORS

Logan Savage and Jess Dison
